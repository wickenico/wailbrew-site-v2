// Stub file to prevent Next.js navigation imports from breaking Vite build
// This is needed because @vercel/speed-insights has Next.js-specific code
// that Vite tries to resolve even when using the React entry point

export const useParams = () => ({});
export const usePathname = () => "";
export const useRouter = () => ({});
export const useSearchParams = () => ({});

